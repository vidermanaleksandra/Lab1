# ## Отчет по лабораторной работе № 2

#### № группы: `ПМ-2501`

#### Выполнила: `Видерман Александра Сергеевна`

#### Вариант: `3`

### Cодержание:

- [Постановка задач](#1-постановка-задач)
- [Входные и выходные данные](#2-входные-и-выходные-данные)
- [Выбор структуры данных](#3-выбор-структуры-данных)
- [Программы](#4-программы)
- [Анализ правильности решений](#5-анализ-правильности-решений)

### 1. Постановка задач
### задача № 1
Какие значения принимает последовательность при разных a0 и n (где a0 - нулевой член, а n - число следующих членов последовательности) вывести закономерность, по которой строится последовательность и запрограммировать ее самостоятельно. В файле с заданием будем вводить значения a0 и n, чтобы узнавать какую последовательность нужно будет запрограммировать.

### задача № 2 
С клавиатуры вводится заглавная буква латинского алфавита и число N (N < 10). Нужно вывести лесенку из букв, которая содержит до N ступеней: в первой строке одна буква, начиная с введенной, во второй – две следующие, в третьей – три следующие и т.д. Если алфавит заканчивается, построение лесенки останавливается на предыдущей полной ступени. Ступени с четным номером печатаются в обратном порядке.

### задача № 3
С клавиатуры вводятся целые числа. Вывести на экран количество тех первых введенных чисел, которые составляют арифметическую прогрессию. При обнаружении конца арифметической прогрессии остальные числа можно не вводить.

### задача № 4
Дан массив из n положительных чисел. Заменить каждое число в массиве на количество его делителей (включая 1 и само число) и вывести полученный массив.
Формат ввода
первая строка: число n
вторая строка: n положительных целых чисел

### 2. Входные и выходные данные
### задача № 1
#### Данные на вход
На вход программа должна получать 2 целых числа. Также даны верхняя и нижняя границы получаемых
чисел.

|              | Тип                | min значение    | max значение   |
|--------------|--------------------|-----------------|----------------|
| a0 (Число 1) |    Целое число     | -2<sup>31</sup> | 2<sup>31</sup> |
| n   (Число 2)|    Целое число     |        1        | 2<sup>31</sup> |

#### Данные на выход
Так как программа должна вывести последовательность целых чисел,  то значения каждого элемента последовательности не будут противоречить границам нулевого члена последовательности.

|                                 |   Тип               | min значение    | max значение   |
|---------------------------------|---------------------|-----------------|----------------|
|    Элемент последовательности   |    Целое число      | -2<sup>31</sup> | 2<sup>31</sup> |

### задача № 2
#### Данные на вход
Программа примает два значения: буквы латинского алфавита для построения лесенки и целые числа, обозначающие количество ступенек. 

|               | Тип                     | min значение    | max значение   |
|---------------|-------------------------|-----------------|----------------|
| start (Буква) |Буква латинского алфавита|        A        |        Z       |
|   N   (Число) |       Целое число       |        1        |       10       |

#### Данные на выход
Программа выводит лесенку из букв латинского алфавита.

|                                 |   Тип                   | min значение  | max значение   |
|---------------------------------|-------------------------|---------------|----------------|
|    Элемент последовательности   |Буква латинского алфавита|       A       |       Z        |

### задача № 3
#### Данные на вход 
Программа принимает последовательность целых чисел.

|                                 |   Тип               | min значение    | max значение   |
|---------------------------------|---------------------|-----------------|----------------|
|    Элемент последовательности   |    Целое число      | -2<sup>31</sup> | 2<sup>31</sup> |

#### Данные на выход
Программа выводит одно целое число, которое обозначает длину начального отрезка последовательности, образующего арифметическую прогрессию.

|               | Тип                     | min значение    | max значение                                   |
|---------------|-------------------------|-----------------|------------------------------------------------|
|   N   (Число) |       Целое число       |        2        |равное количеству элементов в последовательности|

### задача № 4
#### Данные на вход
Программа принимает одно целое число n, обозначающее количество элементов в массиве и n положительных целых чисел, разделённых пробелами.

|                       | Тип                     | min значение    | max значение   |
|-----------------------|-------------------------|-----------------|----------------|
|     n    (Число)      |       Целое число       |        1        | 2<sup>31</sup> |
| n положительных чисел |       Целые числа       | -2<sup>31</sup> | 2<sup>31</sup> |

#### Данные на выход
Программа выводит массив из n целых чисел, обозначающий количество делителей для каждого исходного элемента, в том же порядке.

|                       | Тип                     | min длина       | max длина                                   |
|-----------------------|-------------------------|-----------------|---------------------------------------------|
|  массив целых чисел   |          массив         |        1        |     равная количеству введенных чисел       |


### 3. Выбор структуры данных

### задача № 1
Программа получает 2 целых числа, не превышающих по модулю 2<sup>31</sup>. Поэтому для их хранения
можно выделить 2 переменныe (`a0`, `n`) типа `int`.

|              | название переменной | Тип (в Java) | 
|--------------|---------------------|--------------|
| a0  (Число 1)| `a0`                | `int`        |
| n (Число 2)  | `n`                 | `int`        | 

### задача № 2
Программа получает целое число, не превышающее 10. Поэтому для его хранения
можно выделить переменную (`N`) типа `int`. Еще программа получает буквы латинского алфавита, для этого можно выделить переменную (`start`) типа char.

|              | название переменной | Тип (в Java) | 
|--------------|---------------------|--------------|
| N  (Число)   | `N`                 | `int`        |
| start        | `start`             | `char`       | 

### задача № 3
Программа принимает последовательность целых чисел, каждое из которых типа `int`. Результат будем хранить в переменной (`N`) типа `int`.

|              | название переменной | Тип (в Java) | 
|--------------|---------------------|--------------|
| N  (Число)   | `N`                 | `int`        |

### задача № 4
Программа получает целое число, для хранения выделим пременную (`n`) типа `int`. Затем программа выводит массив целых чисел, каждое из которых типа `int`.

|              | название переменной | Тип (в Java) | 
|--------------|---------------------|--------------|
| n (Число)    | `n`                 | `int`        | 


### 4. Программы
### задача № 1
```java
import java.io.PrintStream;
import java.util.Scanner;
public class Main {
    public static Scanner in = new Scanner(System.in);
    public static PrintStream out = System.out;
    public static void main(String[] args)  {
        int a0 = in.nextInt();
        int n = in.nextInt();

        int current = a0;
        for (int i = 0; i < n; i++) {
            int step = i % 3;
            if (step == 0 || step == 1) {
                current = current * 2;
            } else { //
                current = current + 1;
            }
            System.out.print(current);
            if (i < n - 1) {
                System.out.print(" ");
            }
        }
        System.out.println();
    }
}
```

### задача № 2
```java
import java.io.PrintStream;
import java.io.IOException;
import java.util.Scanner;
public class Main {
    public static Scanner in = new Scanner(System.in);
    public static PrintStream out = System.out;
    public static void main(String[] args) throws IOException {
        char start = in.next().charAt(0);
        int N = in.nextInt();

        char current = start;

        for (int step = 1; step <= N; step++) {
            if ((int)current + step - 1 > (int)'Z') {
                break;
            }

            if (step % 2 == 1) {
                for (int i = 0; i < step; i++) {
                    System.out.print((char)(current + i));
                }
            }
            else {
                for (int i = step - 1; i >= 0; i--) {
                    System.out.print((char)(current + i));
                }
            }
            System.out.println();
            current = (char)(current + step);
        }
    }
}

```

### задача № 3
```java
import java.io.PrintStream;
import java.util.Scanner;
public class Main {
    public static Scanner in = new Scanner(System.in);
    public static PrintStream out = System.out;
    public static void main(String[] args)  {

        if (! in.hasNextInt()) {
            System.out.println(0);
            return;
        }
        int first = in.nextInt();

        if (! in.hasNextInt()) {
            System.out.println(1);
            return;
        }
        int second = in.nextInt();

        int N = 2;
        int diff = second - first;

        while (in.hasNextInt()) {
            int next = in.nextInt();
            if (next == second + diff) {
                N++;
                second = next;
            } else {
                break;
            }
        }
        System.out.println(N);
    }
}
```

### задача № 4
```java
import java.io.PrintStream;
import java.util.Scanner;
public class Main {
    public static Scanner in = new Scanner(System.in);
    public static PrintStream out = System.out;
    public static void main(String[] args)  {

        int n = in.nextInt();
        int[] arr = new int[n];

        for (int i = 0; i < n; i++) {
            arr[i] = in.nextInt();
        }

        for (int i = 0; i < n; i++) {
            arr[i] = countDivisors(arr[i]);
        }

        for (int i = 0; i < n; i++) {
            if (i > 0) System.out.print(" ");
            System.out.print(arr[i]);
        }
        System.out.println();
    }

    public static int countDivisors(int x) {
        int count = 0;
        for (int i = 1; i * i <= x; i++) {
            if (x % i == 0) {
                if (i * i == x) {
                    count++;
                } else {
                    count += 2;
                }
            }
        }
        return count;
    }
}
```

### 5. Анализ правильности решений

1. Тест 1:

    - **Input**:
        ```
        1
        4
        ```

    - **Output**:
        ```
        2 4 5 10
        ```

2. Тест 2:

    - **Input**:
        ```
        D
        5
        ```

    - **Output**:
        ```
        D
        FE
        GHI
        MLKJ
        NOPQR
        ```

3. Тест 3:

    - **Input**:
        ```
        4
        5
        3
        ```

    - **Output**:
        ```
        2
        ```

4. Тест 4:

    - **Input**:
        ```
        3
       -1
       -3
        5
        ```

    - **Output**:
        ```
        0 0 2

        ```
